process Demo =  {
  #keep the logging output to a nice level
  include "FWCore/MessageLogger/data/MessageLogger.cfi"

source = PoolSource { untracked vstring fileNames = {"file:/afs/cern.ch/cms/Tutorials/july06/VertexReconstruction/sim_rec_10muons_1-10GeV_500evts.root"} }

include "MagneticField/Engine/data/volumeBasedMagneticField.cfi"

module simpleVertexAnalysis = KVFTest {
     string TrackLabel = "CTFWMaterial"
     untracked string outputFile="simpleVertexAnalyzer.root"
}

path p = {simpleVertexAnalysis } 
} 
